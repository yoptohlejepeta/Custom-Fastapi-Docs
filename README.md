# Custom Fastapi Docs

Custom docs for Fastapi endpoints

## Commands

```bash
# Run the app
uvicorn app.main:app --reload
```

```bash
# Update tailwind css
cd app
tailwindcss -i styles/main.css -o static/css/main.css --watch
```
